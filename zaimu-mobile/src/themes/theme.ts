import colors from './colors'
import {screenSpace, spacing} from './dimensions'
import {fontStyles} from './typography'

const theme = {
    colors,
    spacing,
    fontStyles,
    screenSpace
}

export default theme